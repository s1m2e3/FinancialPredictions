# downloaded price cache, re-created on the first run
data/
__pycache__/
# btind bank store and proposal weights for the portfolio world
runs_bt/
# trained trees stay private: run checkpoints, seed trees, training logs that print the
# arms, the report and the tree files (portfolio_bt.py writes all of them here)
results/portfolio_bt*/
*.tmp.png
*.tmp.npz
